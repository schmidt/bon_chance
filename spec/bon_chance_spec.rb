require "spec_helper"

describe BonChance do
  it "has a version number" do
    expect(BonChance::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
